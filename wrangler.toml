name = "bambou-resort-booking-engine"
main = "worker/index.ts"
compatibility_date = "2025-05-01"
# fetch() est global sur le runtime Workers — pas besoin de nodejs_compat.

# Static Assets : le front buildé (dist/) est servi par le binding ASSETS.
# not_found_handling = SPA → /confirmation et autres routes client renvoient index.html.
[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "single-page-application"

# ⚠️ PRODUCTION — Bambou Resort Martinique (identifiants publics, NON secrets).
# Le secret MEWS_CLIENT n'est PAS ici : .dev.vars en local, secret Cloudflare en prod.
# ⚠️⚠️ Une réservation créée depuis cette config est une VRAIE réservation dans le PMS.
[vars]
MEWS_BASE_URL = "https://api.mews.com"
MEWS_APP_BASE_URL = "https://app.mews.com"
MEWS_HOTEL_ID = "d81c0909-af62-4227-9cfe-b2cf00fea600"
MEWS_CONFIG_ID = "43ec5bf8-09e2-4ae8-83d2-b39900e86b9b"
# Catégories d'âge réelles de l'entreprise (vérifiées en live).
MEWS_ADULT_AGE_CATEGORY_ID = "3b9bdb28-d9e1-4fac-904f-b2cf00febb8f"
MEWS_CHILD_AGE_CATEGORY_ID = "5cd331e0-0069-4f46-a20f-b2cf00febb8f"

# ★ UNIQUE endpoint de suivi (funnel complet) → n8n → Supabase. TOUS les events y passent.
# Repo public → l'URL est visible ; pour la garder privée, la définir en Secret Cloudflare
# (dashboard) et retirer cette ligne.
WEBHOOK_EVENTS = "https://n8n.srv842183.hstgr.cloud/webhook/booking-event"
# Webhook n8n DÉDIÉ aux demandes de villa (notif équipe / CRM), distinct du flux paniers.
# ⚠️ Créer le node Webhook correspondant dans n8n (chemin ci-dessous) ou ajuster l'URL.
WEBHOOK_VILLA = "https://n8n.srv842183.hstgr.cloud/webhook/villa-lead"

# Supabase (back-office) — URL + clé ANON, toutes deux PUBLIQUES par conception (la clé anon
# vit déjà dans le front du dashboard ; c'est le RLS qui protège). Sert au endpoint /api/mews/villas
# (lecture publique de la table `villas`, éditée depuis le dashboard). JAMAIS la clé service_role ici.
SUPABASE_URL = "https://wrakgyuiihxlcaxinckm.supabase.co"
SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndyYWtneXVpaWh4bGNheGluY2ttIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODU4MzAxNjQsImV4cCI6MjEwMTQwNjE2NH0.UTNSPUh40XWfUbEfz2YL78IUn02FG8kXSauiOgtVahs"
